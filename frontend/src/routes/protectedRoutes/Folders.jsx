import { useAuth } from "../../AuthContext";
import { supabase } from "../../../supabaseConfig";

const Folders = () => {
  return (
    <>
      <div>folders</div>
    </>
  );
};

export default Folders;
